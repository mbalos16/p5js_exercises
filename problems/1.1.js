//HELLO CANVAS//

/* 
enunciado:
El canvas es nuestro lienzo en blanco y puede ocupar el tamaño que queramos dentro de una página HTML. ¡Vamos a probar! Modifica el siguiente código para que el canvas tenga un tamaño de 1024 x 500 y cambia el color de fondo a #A6D8DF.

Pasos
1. Modifica el tamaño del canvas
Modifica el tamaño del canvas para que tenga un tamaño de 1024 x 500

2. Cambia el color de fondo del canvas
Cambia el color de fondo a #A6D8DF
*/

function setup() {
    //Se ha cambiado el ancho y el alto del canvas por los que se han dado.
    createCanvas(1024, 500); 

    //Se ha cambiado el color de fondo en escala de grises de (120) al color de fondo hexadecimal: #A6D8DF. Para asegurarnos que funcciona se ha puesto en strings. 
    background("#A6D8DF"); 
    }

  function draw() {
  }